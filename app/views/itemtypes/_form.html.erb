<%= form_for(@itemtype) do |f| %>
  <% if @itemtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@itemtype.errors.count, "error") %> prohibited this itemtype from being saved:</h2>

      <ul>
      <% @itemtype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :limitdistance %><br>
    <%= f.text_field :limitdistance %>
  </div>
  <div class="field">
    <%= f.label :memo %><br>
    <%= f.text_area :memo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
